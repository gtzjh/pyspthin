# spThin 的 Python 复刻版需求文档

## 1. 项目目标

构建一个 Python 版 `spThin`，在尽量保留原始 R 包实际行为的前提下，提高其在单机 CPU 环境下处理大规模物种出现点数据的能力。

核心目标：

- 复刻 `spThin` 的核心 thinning 语义，而不是在 v1 中引入完全不同的优化目标。
- 相比原始 R 版基于稠密距离矩阵的实现，显著提升大数据集上的计算效率。
- 支持单机并行。
- 采用现代 Python 工程方式，具备明确类型、严格校验和可维护结构。

## 2. 范围

### v1 范围内

- 只支持经纬度坐标。
- 使用基于球面距离的阈值判断，阈值单位为公里。
- 只考虑单机 CPU 运行。
- 支持按重复次数并行执行。
- 使用现代 Python 项目结构与工具链。
- 使用 Pydantic 做强校验。
- 与原始 R `spThin` 做兼容性对照。
- 在不同数据规模和空间分布下做性能与内存基准测试。

### v1 明确不做

- GPU 加速。
- 分布式运行。
- 对外支持平面投影坐标。
- 追求与 R 版在同一随机种子下逐条记录完全一致。

## 3. 兼容性目标

Python 版应尽量保持与 `spThin` 一致的算法语义。

这意味着：

- 保留 `thin.algorithm()` 的高层贪心策略。
- 通过 `reps` 次随机重复来探索不同的 tie-breaking 路径。
- 输出结果按保留记录数从大到小排序。
- 对同一批输入数据、同一阈值、同一重复次数，最终保留记录数应与 R 版非常接近。

这不意味着：

- Python 和 R 的随机数流必须逐位一致。
- 每次 replicate 中保留下来的记录身份必须完全相同。

## 4. 核心算法要求

v1 需要尽量贴近原始 `spThin` 的逻辑：

1. 构建“冲突关系”：任意两点之间距离小于 thinning 阈值时，视为冲突。
2. 对每个 replicate，反复删除当前“冲突数最多”的点。
3. 如果有多个点并列冲突数最大，则随机删除其中一个。
4. 当不存在任何冲突时停止。
5. 保存每个 replicate 的结果，并按保留记录数降序排序。

重要设计规则：

- v1 不应将该贪心算法替换为最大独立集求解、模拟退火、Poisson-disk sampling 或其他不同目标的算法。

原因：

- 这些方法可能在某些意义上更优，但会破坏“结果尽量接近 `spThin`”这一首要要求。

## 5. 性能策略

原始 R 实现的主要瓶颈是构造完整的两两距离矩阵。

Python 实现应当：

- 避免分配 `N x N` 稠密距离矩阵。
- 改为构造稀疏冲突图。
- 使用适合经纬度坐标的地理邻域搜索结构。
- 在所有 replicate 之间复用同一份冲突图。
- 将互相独立的 replicate 并行执行。

v1 推荐方向：

- 使用支持 haversine 距离的空间索引，如 `scikit-learn` 的 `BallTree`，先找出阈值范围内的邻居。
- 将邻居对转换为紧凑的邻接表示。
- 在该稀疏结构上执行与 `spThin` 一致的贪心删点流程。

## 6. 内存要求

系统必须明确避免在大数据集上出现内存爆炸。

要求：

- 生产代码中不得构造完整稠密距离矩阵。
- 内存占用应主要随冲突边数增长，而不是随 `N^2` 增长。
- 并行执行时，不能无脑为每个 worker 复制一整份大型冲突图。
- 在可行情况下，应优先使用只读共享状态、内存映射或其他避免重复拷贝的方案。
- 当冲突图本身过于稠密、即将产生不可接受的内存压力时，应提前中止并给出明确告警或异常。

注意：

- “稀疏”不等于“绝对安全”。如果阈值非常大或点极度聚集，冲突图本身仍可能非常大。

## 7. 并行执行要求

并行必须做，但本项目不实现自动调度器，也不自动根据数据规模切换并行维度。

并行模式由用户显式指定。

要求：

- 支持可配置的 worker 数量，例如 `n_jobs`。
- 支持显式并行模式，例如 `parallel_mode="rep"` 或 `parallel_mode="species"`。
- 默认并行模式为 `parallel_mode="rep"`。
- 在给定 master seed 的前提下，无论 `n_jobs` 如何设置，都应保持可复现。
- 需要设计显式的 seed 拆分机制，使每个 replicate 使用独立且可追踪的随机流。
- `n_jobs=1` 和 `n_jobs>1` 时，在同一 master seed 下应得到统计意义上一致且可复现的结果。

两种并行模式的语义：

- `rep`：对单个物种的多个 replicate 并行。适合单物种分析或物种数量较少的情况。
- `species`：对多个物种并行，每个 worker 负责完整处理一个物种。适合大批量多物种任务。

明确限制：

- 不实现 `parallel_mode="auto"`。
- 不实现动态任务分级、任务打包、负载均衡或内存预算调度器。
- 不默认启用嵌套并行。

非必需项：

- 单个 replicate 内部的并行不是必需要求，除非后续能在不显著增加复杂度的情况下加入。

## 8. 坐标系统要求

v1 对外 API 只接受地理坐标：

- 经度范围 `[-180, 180]`
- 纬度范围 `[-90, 90]`
- thinning 阈值单位为公里

实现要求：

- 内部需要统一单位，并明确记录所采用的地球半径常数。
- 需要严格说明比较规则：只有当距离严格小于阈值时，才算冲突，这一点应与 R 版保持一致。

面向未来的可扩展性要求：

- 内部架构应将“距离计算/邻域搜索后端”和“thinning 主算法”解耦，以便未来加入平面投影支持时无需重写核心算法。
- 但 v1 中不对外启用投影坐标支持。

## 9. 校验与类型安全

实现应采用现代 Python 校验体系。

要求：

- 使用 `venv` 作为隔离环境。
- 使用 `pip` 安装依赖，并建议配合 `pip-tools` 做依赖锁定与开发环境管理。
- 使用 Pydantic v2 对用户输入和结构化输出做运行时校验。
- 全面使用 Python 类型注解。
- 对合理的小问题可做安全矫正；对有歧义或风险的问题必须明确拒绝并报错。

校验规则至少覆盖：

- 必需列是否存在。
- 记录唯一标识列的存在性或自动生成策略。
- 经度和纬度能否转为数值。
- 地理坐标范围是否合法。
- 缺失值处理策略。
- 重复记录处理策略。
- `thin_par`、`reps`、`n_jobs`、输出参数、随机种子等配置项校验。

推荐行为：

- 允许对字符串形式的数字做受控转换。
- 不应在不确定的情况下静默交换经纬度列。
- 若发生自动矫正，应给出明确 warning。
- 如果用户未提供记录唯一标识列，内部应自动生成稳定的 `record_id`，并保证其可回溯到输入数据中的唯一一行。

## 10. API 要求

该包应提供面向 Python 用户、同时又保留 `spThin` 使用习惯的 API。

至少需要提供：

- `thin(...)`
- `thin_many(...)`
- `summary_thin(...)`
- `plot_thin(...)`

`thin(...)` 的预期行为：

- 接受如 pandas DataFrame 这样的表格输入。
- 使用 Pydantic 配置模型校验参数。
- 生成 replicate 级别的 thinning 结果。
- 可选择返回内存中的 replicate 数据集。
- 可选择将最佳结果写出为 CSV。
- 可选择写出日志文件。

`thin_many(...)` 的预期行为：

- 接受包含多个物种的表格输入，并指定 `species_col`。
- 用于批量处理多个物种。
- 由用户显式指定并行模式；当 `parallel_mode="species"` 时，以物种为任务单位并行。
- 不负责自动任务分级或动态调度。
- 允许按物种输出结果与日志。

输出设计要求：

- 返回值应是结构化对象，而不是无类型字典。
- 结果对象应保留足够元数据，以支持复现、汇总和基准分析。
- 结果对象应显式包含每个 replicate 的 `retained_record_ids` 或等价稳定索引。
- 结果对象应支持基于 `retained_record_ids` 恢复原始整行数据，而不是只暴露经纬度子集。

## 10.1 记录身份与附加列保留要求

这一点是相对原始 R `spThin` 必须明确改进的部分。

要求：

- thinning 的内部计算可以只依赖物种列、经度列、纬度列和记录唯一标识列。
- 但输出结果不能只返回物种名和经纬度，而必须能够映射回输入中的原始整行记录。
- 所有被保留的记录，都必须通过稳定的 `record_id` 或等价内部索引与输入数据一一对应。
- 默认输出应保留输入数据中的全部原始列，而不是只输出经纬度和物种列。
- 如果用户要求写出 CSV，默认写出的是“原始列的子集行”，并额外附加必要的运行元数据列。

推荐设计：

- 用户可以显式提供 `record_id_col`。
- 如果用户未提供，则系统内部自动生成一个稳定且唯一的 `record_id`。
- 算法核心仅在内部基于 `record_id -> 行位置` 做筛选。
- 输出阶段根据保留下来的 `record_id` 回表，恢复原始全部列。

附加输出元数据建议至少包括：

- `record_id`
- `replicate_id`
- `replicate_rank`
- `retained_count`
- `species` 或对应物种标识

明确限制：

- 不采用原始 R 包那种“输出时只重建物种列、经度列、纬度列”的设计。
- 不允许因为 thinning 过程而丢失用户输入中的其他列与原始行身份。

## 11. 现代 Python 项目结构

实现应使用现代且易维护的项目组织方式。

推荐基础结构：

- `pyproject.toml`
- `.venv/`
- `requirements.in` / `requirements-dev.in`
- `pip-compile` 生成的锁定文件
- `src/` 布局
- `pytest`
- `ruff`
- 可选静态检查，如 `mypy` 或 `pyright`

推荐模块拆分：

- `models.py` 或 `config.py`：Pydantic 模型
- `validate.py`：输入校验和矫正逻辑
- `distance/`：地理距离和邻域搜索后端
- `graph/`：冲突图构建与存储
- `algorithm/`：thinning 核心逻辑
- `parallel/`：并行调度
- `io/`：CSV 和日志输出
- `plotting/`：诊断图

## 12. 测试要求

测试不仅要覆盖正确性，还要覆盖运行安全性。

### 单元测试

- 距离转换与阈值判定行为。
- 输入校验与自动矫正行为。
- `record_id` 生成、显式 `record_id_col` 校验与回表行为。
- 小规模、可手工验证样例上的图构建。
- 贪心删点逻辑。
- replicate 结果排序。
- 随机种子可复现性。

### 性质或不变量测试

- 最终保留集中的任意两点距离都不应小于阈值。
- replicate 输出不应包含输入集之外的记录。
- 输出结果必须按保留数非增排序。
- 输出中的每一条记录都必须能唯一映射回输入中的原始一行。
- 默认输出必须保留输入中的全部原始列。

### 与 R `spThin` 的回归测试

- 保留一套小型基准数据，可同时在 R 和 Python 中运行。
- 比较保留记录数分布、最大保留数以及 summary 指标。
- 在测试环境可用 R 时，以本仓库中的原始 R 包作为参考实现。

## 13. 兼容性验收标准

逐条记录完全一致不应作为主要验收标准。

主要验收指标应包括：

- Python 和 R 在冲突判定与阈值语义上保持一致。
- 在小型和中型基准数据上，最大保留数通常应完全一致或只存在极小偏差。
- 多次 benchmark 后，保留记录数分布应与 R 版非常接近。
- summary 行为应足够一致，不能因为实现差异显著影响下游生态建模结论。

建议的 v1 量化验收目标：

- 在基准数据上，最大保留数差异不超过 `max(1, 保留数的 1%)`。
- replicate 平均保留数与 R 版的差异不超过 1%。
- replicate 输出最终排序行为一致。

这些阈值是工程验收目标，不保证适用于所有极端数据集。

## 14. 基准测试要求

基准测试不能只看样本量。

测试矩阵至少应覆盖：

- 数据规模：小、中、大、超大
- 空间分布：均匀、聚集、高度聚集
- thinning 半径：稀疏冲突、中等冲突、高密冲突
- worker 数：1、2、4，以及机器允许的更高值

每组 benchmark 至少采集：

- 总运行时间
- 峰值内存
- 冲突边数
- replicate 数
- 保留记录数汇总

重要规则：

- 不能只盯着大 `N`。中等 `N` 但高密冲突的数据，可能比更大的稀疏数据更难处理。

## 15. 运行安全要求

该包应在失败时保持透明和可解释。

要求：

- 尽早识别非法坐标。
- 对不合理或高风险的参数组合，在可能的情况下提前拒绝。
- 如果能够估计内存风险，应在重计算开始前给出告警。
- 日志中应明确记录运行配置、worker 数、随机种子、阈值和汇总结果。

## 16. 风险与注意事项

实现时需要重点关注以下问题：

- 地理距离实现细节必须与 R `spThin` 的阈值语义校准一致。
- Python 多进程如果处理不当，会因为冲突图复制而破坏内存优势。
- 即使不构造稠密矩阵，极高密度冲突图仍可能过大。
- 任何对贪心策略的修改都可能提高速度或质量，但同时降低与 `spThin` 的可比性。
- 并行下的可复现性依赖明确的 RNG 设计，不能依赖全局随机状态。
- 输入校验可以纠正明显问题，但不能静默篡改原始生态数据。

## 17. 第一阶段交付物

第一阶段应至少交付：

- 一个基于 `venv` 的 Python 包骨架及安装说明。
- 仅支持经纬度输入的 `thin(...)` 与 `thin_many(...)` API。
- 基于地理邻域搜索的稀疏冲突图构建器。
- 单机 CPU 下用户显式指定的 `rep` / `species` 并行能力。
- 基于 `record_id` 的稳定记录回表与原始列保留机制。
- 结构化结果对象，以及 CSV / 日志输出。
- `summary_thin(...)` 与 `plot_thin(...)`。
- 单元测试、回归测试和 benchmark 脚本。
- 与原始 R 包对比的文档化工作流。

## 18. 当前已确定的决策

本项目当前已经确定的原则：

- v1 只支持经纬度输入。
- v1 必须保留 `spThin` 的贪心语义。
- 只考虑单机 CPU。
- 默认并行模式为 `rep`。
- 允许用户显式切换到 `species` 并行。
- 不实现自动调度或动态负载均衡。
- 必须通过 `record_id` 或等价稳定索引保留输入记录身份，并在输出中恢复原始整行数据。
- 使用 `venv` 构建隔离环境。
- 使用 Pydantic 做输入输出校验。
- 与 R `spThin` 的兼容性是首要目标之一，而不是后补工作。

## 19. 建议技术选型

下面是基于当前需求的建议技术栈，优先级按照“与 `spThin` 的可比性 + 工程稳定性 + 性能收益”综合排序。

### 19.1 运行时依赖

- Python 3.12
- `numpy`
- `pandas`
- `scipy`
- `scikit-learn`
- `pydantic` v2
- `matplotlib`
- `psutil`

说明：

- `numpy` 负责底层数组与数值计算。
- `pandas` 负责用户输入输出的数据表接口。
- `scipy` 可用于稀疏矩阵、辅助数据结构和部分数值工具。
- `scikit-learn` 提供适合经纬度半径搜索的 `BallTree`。
- `pydantic` v2 用于配置、输入和输出结果校验。
- `matplotlib` 用于复刻 `plotThin`。
- `psutil` 用于 benchmark 和运行时内存监控。

### 19.2 邻域搜索后端建议

v1 首选：

- `scikit-learn` 的 `BallTree(metric="haversine")`

原因：

- 对经纬度语义更直接，避免先投影再近似。
- 能高效做 radius query，适合先构造冲突边，再复用到多次 replicate。
- 官方文档明确 `BallTree.valid_metrics` 支持 `haversine`。

工程建议：

- 将 `scikit-learn` 设计为“核心依赖”或“可选后端依赖”都可以，但从当前需求看，我更倾向于直接作为核心依赖。

### 19.3 并行方案建议

推荐优先级：

1. `joblib`
2. 标准库 `concurrent.futures.ProcessPoolExecutor`

建议：

- 如果使用 `scikit-learn`，那么 `joblib` 已经在依赖链中，直接用它做 replicate 并行最顺手。
- 如果后续想严格控制共享内存和 worker 生命周期，可以再下沉到标准库多进程实现。

并行策略建议：

- 默认模式设为 `parallel_mode="rep"`。
- 当用户明确要批量处理大量物种时，可手动切换为 `parallel_mode="species"`。
- 不提供自动模式 `auto`。
- 不实现动态调度器。

### 19.4 测试与质量工具

- `pytest`
- `hypothesis`
- `ruff`
- `mypy` 或 `pyright`

说明：

- `hypothesis` 适合做距离阈值、输出不变量和随机数据集上的性质测试。
- `ruff` 负责 lint 和基础格式规范。
- `mypy` 或 `pyright` 用于补足 Pydantic 之外的静态类型约束。

### 19.5 可选性能增强

v1 先不强制，但可作为后续优化方向：

- `numba`

适用场景：

- 如果 profiling 证明瓶颈已经不在邻域搜索，而在 replicate 内部的贪心删点循环，则可以考虑把稀疏邻接结构和删除更新逻辑下沉到 `numba`。

不建议在 v1 一开始就引入的原因：

- 会提高实现复杂度。
- 会让调试和类型边界更复杂。
- 在尚未完成基线版本前，容易过早优化。

## 20. 推荐实现策略

建议分两层实现：

### 第一层：语义对齐版

- 用 `BallTree` 构造冲突图。
- 用稀疏邻接表保存冲突关系。
- 用与 `spThin` 一致的“删最大冲突度节点”策略执行 thinning。
- 先保证结果分布与 R 版接近。
- 并行策略只提供用户显式指定的 `rep` 模式和 `species` 模式。

### 第二层：性能增强版

- 在不改变算法语义的前提下，优化邻接存储、度数更新和并行调度。
- 只有在基准测试确认瓶颈后，再考虑 `numba` 或更底层的数据结构。

## 21. 与原始 spThin 的实现差异

如果按上面的方案实现，Python 版与 R 版的主要差异将是“实现方式不同，但算法语义尽量相同”。

应保持相同的部分：

- 冲突定义：距离严格小于阈值。
- replicate 逻辑：每次从同一初始冲突图出发。
- 贪心规则：每轮删当前冲突数最大的点。
- tie-breaking：并列最大时随机删除一个。
- 输出排序：按保留记录数降序。

会不同的部分：

- R 版先构造稠密两两距离矩阵；Python 版应改为稀疏冲突图。
- R 版主要依赖 `fields::rdist.earth`；Python 版应改为基于 haversine 的邻域搜索。
- Python 版会显式支持并行 replicate。
- Python 版会增加更严格的输入输出校验、记录身份保留与可追踪元数据。

结论：

- 这是“同语义复刻”，不是“逐行翻译”。
- 只要冲突图构造正确，后续贪心逻辑保持一致，结果通常应与 R `spThin` 非常接近。
